---
import BaseLayout from '~/layouts/BaseLayout.astro';
import BookGrid from '~/components/BookGrid.astro';
import Pagination from '~/components/Pagination.astro';
import { getAllBooks, paginate } from '~/data';


const pageParam = Astro.params.page;
const books = getAllBooks();
const totalPages = Math.max(1, Math.ceil(books.length / 30));

if (!pageParam || !/^[1-9]\d*$/.test(pageParam)) {
  return new Response(null, { status: 404 });
}

const pageNum = Number(pageParam);
if (!Number.isSafeInteger(pageNum) || pageNum < 2 || pageNum > totalPages) {
  return new Response(null, { status: 404 });
}

const { items, page, total } = paginate(books, pageNum);
---

<BaseLayout title={`Halaman ${page}`} description={`Halaman ${page} dari ${totalPages} — ${total} buku dalam katalog Pustaka Taratsa`} canonical="/page" noindex ogType="website">
  <h1>Halaman {page} dari {totalPages}</h1>
  <p>{total} buku dalam katalog.</p>

  <BookGrid books={items} cols={5} />

  <Pagination page={page} totalPages={totalPages} baseUrl="/page" />
</BaseLayout>
